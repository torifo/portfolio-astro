---
// AboutSection.astro - 自己紹介セクション（microCMS連携）
import { fetchProfile } from '../../lib/microcms';

const profile = await fetchProfile();

// Connect セクションのメールアドレス（コピーボタン付きで表示）
const mails: { label: string; value: string }[] = [
  { label: '採用・就活', value: profile?.occupation_mail ?? '' },
  { label: '開発・技術', value: profile?.develop_mail ?? '' },
].filter((mail) => mail.value !== '');

const getTagBorderColor = (tags: string[]) => {
  const first = tags[0];
  const map: Record<string, string> = {
    'Academic':   'border-cyan-400',
    'Technology': 'border-purple-400',
    'Opus':       'border-green-400',
    'Pulse':      'border-orange-400',
    'Community':  'border-pink-400',
  };
  return map[first] || 'border-gray-400';
};

const getTagBadgeColor = (tag: string) => {
  const map: Record<string, string> = {
    'Academic':   'bg-cyan-500/20 text-cyan-300',
    'Technology': 'bg-purple-500/20 text-purple-300',
    'Opus':       'bg-green-500/20 text-green-300',
    'Pulse':      'bg-orange-500/20 text-orange-300',
    'Community':  'bg-pink-500/20 text-pink-300',
  };
  return map[tag] || 'bg-gray-500/20 text-gray-300';
};
---

<section id="about" class="py-20 relative">
  <div class="container mx-auto px-6">
    <!-- セクションタイトル -->
    <div class="text-center mb-10">
      <h2 class="text-4xl md:text-5xl font-bold gradient-text mb-4">About Me</h2>
      <p class="text-gray-400 text-lg max-w-2xl mx-auto">
        {profile?.introduction ?? 'プロフィールを読み込み中...'}
      </p>
    </div>

    <!-- 経歴タグフィルター -->
    <div class="mb-12 text-center">
      <div class="flex flex-wrap justify-center gap-3">
        <button
          class="px-4 py-2 bg-white/10 text-white rounded-full text-sm font-medium transition-all hover:bg-white/20 cursor-pointer about-tag-btn active-about-tag"
          data-tag="all"
          type="button"
        >
          All
        </button>
        {(['Academic', 'Technology', 'Opus', 'Pulse', 'Community'] as const).map((tag) => (
          <button
            class={`px-4 py-2 ${getTagBadgeColor(tag)} rounded-full text-sm font-medium transition-all hover:opacity-100 opacity-60 cursor-pointer about-tag-btn`}
            data-tag={tag}
            type="button"
          >
            {tag}
          </button>
        ))}
      </div>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-start">
      <!-- 左側: プロフィール画像・基本情報 -->
      <div class="space-y-6">
        <!-- アバター -->
        <div class="relative w-56 h-56 mx-auto lg:mx-0">
          <div class="absolute inset-0 bg-gradient-to-br from-cyan-400 via-purple-500 to-pink-500 rounded-2xl animate-pulse-glow"></div>
          <div class="absolute inset-2 bg-gray-800 rounded-2xl overflow-hidden">
            {profile?.avatar ? (
              <img
                src={profile.avatar.url}
                alt={`${profile.name} avatar`}
                class="w-full h-full object-cover"
              />
            ) : (
              <img
                src="/images/toriforium_circle.png"
                alt="avatar"
                class="w-full h-full object-cover"
              />
            )}
          </div>
        </div>

        <!-- 名前・肩書き -->
        <div class="glass p-6 rounded-2xl">
          <h3 class="text-2xl font-bold text-white mb-1">
            {profile?.name ?? 'とりふぉりうむ'}
          </h3>
          <p class="text-cyan-400 text-sm mb-4 leading-relaxed">
            {profile?.title ?? ''}
          </p>
          <p class="text-gray-300 text-sm leading-relaxed">
            Python, SQL, JavaScript, Java を扱い、Discord Bot・Web アプリ・CLI ツールなどを開発しています。
            現在は情報推薦システムにおけるプライバシー問題の研究に取り組んでいます。
          </p>
        </div>

        <!-- Connect -->
        <div id="connect" class="glass p-6 rounded-2xl scroll-mt-24">
          <h3 class="text-xl font-bold text-white mb-4 flex items-center">
            <svg class="w-5 h-5 mr-3 text-emerald-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13.828 10.172a4 4 0 00-5.656 0l-4 4a4 4 0 105.656 5.656l1.102-1.101m-.758-4.899a4 4 0 005.656 0l4-4a4 4 0 00-5.656-5.656l-1.1 1.1"></path>
            </svg>
            Connect
          </h3>
          <div class="space-y-3">
            <!-- GitHub -->
            <a
              href="https://github.com/torifo"
              target="_blank"
              rel="noopener noreferrer"
              class="flex items-center text-gray-300 hover:text-cyan-400 transition-colors"
            >
              <svg class="w-5 h-5 mr-3 shrink-0" fill="currentColor" viewBox="0 0 24 24">
                <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
              </svg>
              <span>github.com/torifo</span>
            </a>
            <!-- メールアドレス（採用・就活 / 開発・技術）-->
            {mails.map((mail) => (
              <div class="flex items-center">
                <a
                  href={`mailto:${mail.value}`}
                  class="flex items-center min-w-0 flex-1 text-gray-300 hover:text-cyan-400 transition-colors"
                >
                  <svg class="w-5 h-5 mr-3 shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
                  </svg>
                  <div class="min-w-0">
                    <div class="text-xs text-gray-500 leading-none mb-0.5">{mail.label}</div>
                    <div class="truncate">{mail.value}</div>
                  </div>
                </a>
                <button
                  type="button"
                  class="copy-mail-btn ml-2 shrink-0 p-2 rounded-lg text-gray-400 hover:text-cyan-400 hover:bg-white/10 transition-colors cursor-pointer"
                  data-mail={mail.value}
                  aria-label={`${mail.label}のメールアドレスをコピー`}
                  title="コピー"
                >
                  <svg class="copy-icon-default w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z"/>
                  </svg>
                  <svg class="copy-icon-done w-4 h-4 hidden text-cyan-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                  </svg>
                </button>
              </div>
            ))}
            <span id="copy-mail-status" class="sr-only" aria-live="polite"></span>
          </div>
        </div>
      </div>

      <!-- 右側: 経歴タイムライン -->
      <div class="glass p-6 rounded-2xl">
        <h3 class="text-xl font-bold text-white mb-6 flex items-center">
          <svg class="w-5 h-5 mr-3 text-cyan-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          経歴
        </h3>

        <div class="space-y-4 max-h-[520px] overflow-y-auto pr-2 scrollbar-thin" id="about-timeline">
          {profile?.history.map((item) => (
            <div
              class={`border-l-2 ${getTagBorderColor(item.tag)} pl-4 about-history-item`}
              data-tags={item.tag.join(',')}
            >
              <div class="flex items-center gap-2 mb-1">
                <span class="text-white font-semibold text-sm">{item.date}</span>
                <div class="flex gap-1 flex-wrap">
                  {item.tag.map((t) => (
                    <span class={`px-1.5 py-0.5 ${getTagBadgeColor(t)} rounded text-xs`}>{t}</span>
                  ))}
                </div>
              </div>
              <p class="text-gray-300 text-sm leading-relaxed">{item.event}</p>
            </div>
          ))}
        </div>
      </div>
    </div>
  </div>

  <!-- セクション区切り線 -->
  <div class="section-divider"></div>
</section>

<script is:inline>
  document.addEventListener('DOMContentLoaded', function () {
    const tagButtons = document.querySelectorAll('.about-tag-btn');
    const historyItems = document.querySelectorAll('.about-history-item');

    tagButtons.forEach(function (btn) {
      btn.addEventListener('click', function () {
        const selectedTag = this.getAttribute('data-tag');

        // ボタンのアクティブ状態を更新
        tagButtons.forEach(function (b) {
          b.classList.remove('active-about-tag');
          if (b.getAttribute('data-tag') === 'all') {
            b.classList.remove('bg-white/30');
            b.classList.add('bg-white/10');
          } else {
            b.classList.add('opacity-60');
            b.classList.remove('opacity-100');
          }
        });
        this.classList.add('active-about-tag');
        if (selectedTag === 'all') {
          this.classList.remove('bg-white/10');
          this.classList.add('bg-white/30');
        } else {
          this.classList.remove('opacity-60');
          this.classList.add('opacity-100');
        }

        // 経歴アイテムの表示/非表示
        historyItems.forEach(function (item) {
          if (selectedTag === 'all') {
            item.style.display = '';
          } else {
            const tags = item.getAttribute('data-tags').split(',');
            item.style.display = tags.includes(selectedTag) ? '' : 'none';
          }
        });
      });
    });

    // メールアドレスのコピー
    const copyButtons = document.querySelectorAll('.copy-mail-btn');
    const copyStatus = document.getElementById('copy-mail-status');

    function fallbackCopy(text) {
      const textarea = document.createElement('textarea');
      textarea.value = text;
      textarea.setAttribute('readonly', '');
      textarea.style.position = 'fixed';
      textarea.style.opacity = '0';
      document.body.appendChild(textarea);
      textarea.select();
      let copied = false;
      try {
        copied = document.execCommand('copy');
      } catch (e) {
        copied = false;
      }
      document.body.removeChild(textarea);
      return copied;
    }

    copyButtons.forEach(function (btn) {
      const defaultIcon = btn.querySelector('.copy-icon-default');
      const doneIcon = btn.querySelector('.copy-icon-done');
      let resetTimer = null;

      function showCopied() {
        defaultIcon.classList.add('hidden');
        doneIcon.classList.remove('hidden');
        btn.setAttribute('title', 'コピーしました');
        if (copyStatus) {
          copyStatus.textContent = 'メールアドレスをコピーしました';
        }
        clearTimeout(resetTimer);
        resetTimer = setTimeout(function () {
          defaultIcon.classList.remove('hidden');
          doneIcon.classList.add('hidden');
          btn.setAttribute('title', 'コピー');
          if (copyStatus) {
            copyStatus.textContent = '';
          }
        }, 1600);
      }

      btn.addEventListener('click', function (event) {
        event.preventDefault();
        const mail = btn.getAttribute('data-mail');
        if (!mail) return;

        if (navigator.clipboard && window.isSecureContext) {
          navigator.clipboard.writeText(mail).then(showCopied, function () {
            if (fallbackCopy(mail)) showCopied();
          });
        } else if (fallbackCopy(mail)) {
          showCopied();
        }
      });
    });
  });
</script>

<style>
  .scrollbar-thin {
    scrollbar-width: thin;
    scrollbar-color: rgba(100, 116, 139, 0.4) transparent;
  }
  .scrollbar-thin::-webkit-scrollbar {
    width: 4px;
  }
  .scrollbar-thin::-webkit-scrollbar-track {
    background: transparent;
  }
  .scrollbar-thin::-webkit-scrollbar-thumb {
    background-color: rgba(100, 116, 139, 0.4);
    border-radius: 2px;
  }
</style>
